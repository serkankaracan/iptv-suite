# Windows development ve M1–M7 foundations

M5 network/transport foundation, M6 Xtream-compatible Live TV adapter, M7 remote M3U/M3U8 streaming parser ve M8 local persistence/cache/indexing `COMPLETED, 2026-08-21`; M9 categories/channels query ve virtualized UI sıradaki aktif milestone'dur. Streaming loader production same-SQLite-transaction sink'e yazar; protected locator, atomik active snapshot, query/cache, prune ve reconciliation uygulanmıştır. Clean 5k–50k ×20 Decision koşusunda 50k import p95 `2,739 s`, allocation p95 `116,330 MiB`, working-set delta p95 `6,164 MiB` ve cancellation p95 `12,390 ms` ölçülmüştür. Ayrı process crash harness'ı açık refresh transaction'ı öldürüldükten sonra önceki complete snapshot'ı ve inert/no-sidecar recovery sonucunu doğrular. Ayrıntı: [M8 completion evidence](../../docs/quality/M8_COMPLETION_EVIDENCE.md).

Aşağıdaki M4 comparative zaman çizelgesindeki M8 `Proposed`/unimplemented/açık ifadeleri tarihsel karar anını anlatır; güncel M8 `COMPLETED` ve ADR-003 `Accepted` kaydını geçersiz kılmaz.

Bu klasör Windows uygulamasını, M2 test scaffold'unu, M3 saf domain/validation çekirdeğini ve M4'ün protected-storage dilimini içerir. M2 hosted kabulü ve M3 local mühendislik kabulü **PASS, 2026-08-09** durumundadır. M4 foundation `COMPLETED, 2026-08-20`; 2026-08-13 hosted run `31735655363`, source-draft protection, packaged composition ve startup temp-aday cap'i dahil tarihsel v1 store snapshot'ında 153/153 testi iki deterministik koşuda; signed packaged-smoke ve required coordinator'ı başarıyla tamamlamıştır. Internal source-payload decoder ve semantic-owner-bound store v2 snapshot'ı 171/171 testi geçmiştir. Exact configuration-record deletion primitive'i dahil tarihsel local snapshot architecture 13, unit 137 ve integration 33 olmak üzere 183/183 testi iki deterministik koşuda geçmiştir. DPAPI user-boundary harness'ı ve statik contract guard'ları dahil güncel local worktree exact-SDK gate'i Debug/Release x64 build'leri 0 warning/error; architecture 16, unit 154 ve integration 34 olmak üzere 204/204 testi iki identical `Passed` setiyle; fixture determinism, sentinel fail/recovery, scanner CLI self-test ve artifact canary kontrolleriyle geçmiştir. `19.626` byte local summary SHA-256 `c25996dea148d3c131f84bfa215e2ebd0122790fb3058bd71d5969e97953652f` ve `commitSha=null` taşır; bu local sonuç tek başına commit-bound hosted/package kanıtı değildir. Full gate harness'ı derler ve statik isolation/security contract'ını denetler, gerçek local-user controller executable'ını çalıştırmaz. 2026-08-14 GitHub Actions UI kaydında run number `#15`, commit `6da2fc92a223c23d07f7a902de7607e16b1cbb8a` için 3/3 işi ve üç artifact'ı başarıyla tamamlamış; ilk v2 hosted quality/package/lifecycle geçişini workflow/UI düzeyinde **VERIFIED** yapmıştır. Aynı tarihli run number `#16` UI kaydı, exact deletion primitive'ini içeren commit `3e806edd9a9482b87cccbf000ac8a81823562f6a` için 3/3 işi yeşil, üç artifact ve `8m24s` toplam süre gösterir; bu takip sonucu da yalnız workflow/UI düzeyinde **VERIFIED**dir. Sonraki run number `#18` UI kaydı, signed `0.0.1.0 → 0.0.2.0` normal update lane'ini ve package output-directory düzeltmesini içeren commit `bfe5960d5bc911e21ba1e1e1675deb575400e531` için 3/3 işi yeşil, üç artifact ve `9m16s` toplam süre gösterir; commit-bound workflow package işi lifecycle betiğini zorunlu adım olarak çalıştırdığından yeni lane hosted execution'ı workflow/UI düzeyinde **VERIFIED**dır. Run number `#20` UI kaydı, reset failure-classification hardening'ini içeren commit `6f30e3fa92f329c1fa26333c591edb5148261cec` için 3/3 işi yeşil, üç artifact ve `8m14s` toplam süre gösterir; zorunlu lifecycle adımı nedeniyle schema-v3 reset/live-state uninstall/reinstall fresh-state zinciri de workflow/UI düzeyinde **VERIFIED**dır. Run number `#21` UI kaydı, channel deletion primitive'ini içeren commit `0ef0da5793dc4fd1f4e83dbe38cd49d7a7ccb56f` için 3/3 işi yeşil, üç artifact ve `9m25s` toplam süre gösterir; channel primitive'in commit-bound hosted execution'ı workflow/UI düzeyinde **VERIFIED**dır. Database run ID, job log/test sayısı ve indirilen artifact alan/ZIP/içerik/tam digest doğrulaması bu kayıtların parçası değildir. Uygulama UI'sı hâlâ yalnız gerçek assembly/package sürümünü, build configuration'ını ve process architecture'ını gösteren development shell'dir; source formu, network/provider, parser, database veya playback özelliği henüz yoktur.

Test-only lifecycle schema v3 dilimi, güncel disposable pakette reset ile app-owned state'in kaldırılmasını ve canlı protected record/control ticket varken exact current-user uninstall ardından aynı güncel MSIX'in fresh-state reinstall'ini kodlar. Run number `#20`, commit `6f30e3fa92f329c1fa26333c591edb5148261cec` için bu dilimin hosted execution'ını workflow/UI düzeyinde **VERIFIED** yapmıştır; artifact içeriği bağımsız doğrulanmamıştır.

Run number `#22` UI kaydı, threat-model kararı ile production `asInvoker` / `uiAccess=false` manifestini içeren commit `43bf88c591bdc216d3a288ca578f90e4fad927c6` için 3/3 işi yeşil, üç artifact ve `8m23s` toplam süre gösterir. Exact architecture gate ile signed production-package smoke ve disposable lifecycle adımı commit-bound workflow'da zorunlu olduğundan declarative least-privilege manifest sınırı ve packaged execution workflow/UI düzeyinde **VERIFIED**dır. Bu kayıt R26 kararının doğruluğunu, runtime process token'ının non-elevated olduğunu, atomik path güvenliğini veya same-user isolation'ı kanıtlamaz; indirilen artifact alanları, ZIP/içerik, tam digest, job log/test sayısı ve GitHub database run ID'si bağımsız doğrulanmamıştır.

Run number `#24` UI kaydı, yalnız documentation değişikliği olan commit `207455a54d2d7ac9b6b5c1ce8eb5e29bbee0c383` için 3/3 yeşil iş, üç artifact ve `9m02s` toplam süre gösterir. Bu workflow/UI kaydı protected-catalog adayının Smoke veya `Decision` execution kanıtı değildir.

Run number `#25` UI kaydı, `test(windows): spike protected catalog layout` başlıklı commit `786151cef6c60de5e34009ebb6643ec804490ee9` için 3/3 işi yeşil, üç artifact ve `8m32s` toplam süre gösterir. Normal hosted workflow solution'ın Debug/Release x64 build'lerinde protected-catalog projesini derlemiş; zorunlu architecture guard'ları production graph ayrımını ve opt-in wrapper'ın quality/workflow tarafından çağrılmamasını denetlemiş, package smoke da çıkarılmış production MSIX'te `IptvSuite.ProtectedCatalogSpike*` denylist'ini uygulamıştır. Bu hosted compile/isolation/package-exclusion sonucu workflow/UI düzeyinde **VERIFIED**dır; aday executable'ının `Smoke` veya `Decision` modu çalıştırılmamış ve candidate evidence upload edilmemiştir. Job log/test sayıları ile indirilen artifact içerik ve tam digest bağları bağımsız doğrulanmamıştır; M4 ve ADR-003 durumları değişmez.

## Toolchain

| Bileşen | Exact sürüm / sınır |
|---|---|
| .NET SDK | `10.0.302` |
| .NET runtime/reference pack | `10.0.10` |
| Windows App SDK | `2.4.0` stable; transitive WinUI `2.3.6` |
| Windows SDK BuildTools | `10.0.26100.8249` stable |
| ProtectedData | `System.Security.Cryptography.ProtectedData` `10.0.10` stable, Infrastructure-only |
| Fake time test paketi | `Microsoft.Extensions.TimeProvider.Testing` `10.8.0` stable, test-only |
| MSTest | `4.3.3` stable |
| OS / architecture | Windows 11 `10.0.26100+`, x64 |

`global.json`, central package versions ve her projedeki `packages.lock.json` birlikte deterministic restore sınırını oluşturur. SDK resolver exact `10.0.302` ister: `rollForward` değeri `disable`, `allowPrerelease` değeri `false` olduğu için başka feature-band/patch veya preview SDK'ya sessiz geçiş yoktur. Preview/Experimental doğrudan bağımlılık yoktur. Resmî `winapp` CLI public preview olduğundan build veya launch zincirine alınmamıştır.

## Restore, build ve test

Bütün komutları repository kökünden çalıştırın:

```powershell
dotnet --version
dotnet restore .\apps\windows\IptvSuite.Windows.sln --locked-mode -p:Platform=x64
dotnet build .\apps\windows\IptvSuite.Windows.sln -c Debug -p:Platform=x64 --no-restore
dotnet build .\apps\windows\IptvSuite.Windows.sln -c Release -p:Platform=x64 --no-restore
dotnet test .\apps\windows\tests\IptvSuite.ArchitectureTests\IptvSuite.ArchitectureTests.csproj -c Release --no-build --no-restore
```

Build içindeki analyzers/code-style denetimi lint gate'idir; ayrı bir linter dependency'si yoktur. SDK sürümü tam `10.0.302` değilse komutlara devam etmeyin.

Yeni package sürümü bilinçli değiştirildiğinde önce normal restore ile lock dosyaları güncellenir, diff incelenir ve ardından yukarıdaki `--locked-mode` akışı tekrar çalıştırılır.

## Visual Studio'da çalıştırma

1. `apps/windows/IptvSuite.Windows.sln` dosyasını açın ve solution platformunu `x64`, configuration'ı `Debug` seçin. **Build > Configuration Manager** içindeki `IptvSuite.Windows` satırında `Build` ve `Deploy` işaretli olmalıdır; solution bu eşlemeleri Debug/Release için source-control altında taşır.
2. `IptvSuite.Windows` projesini **Başlangıç Projesi Olarak Ayarla** seçeneğiyle işaretleyin.
3. Debug target listesinden **IptvSuite.Windows (Package)** profilini seçip `F5` kullanın.

Source-controlled `Properties/launchSettings.json`, Visual Studio'ya packaged single-project MSIX deploy/launch yolu için `MsixPackage` komutunu verir. Profil görünmezse solution'ı kapatıp yeniden açın veya projeyi unload/reload edin. `IptvSuite.Windows (Package)` yerine unpackaged/Project profili eklemeyin; uygulamanın identity ve `LocalCache` davranışı packaged modele bağlıdır.

## M3 domain ve güvenli validation

- `ContentSource`, `PlaylistSnapshot`, `ChannelCategory`, `LiveChannel`, typed ID ve versioned `ChannelStableKey` contract'ları `IptvSuite.Domain` içindedir. `LiveChannel` tam bir oynatma kaynağı olarak ya typed provider item key ya da protected locator reference taşır; M3U `tvg-id` tek başına oynatılabilir sayılmaz.
- Xtream-compatible ve remote playlist girdileri yalnız HTTPS kabul eder. `SafeEndpoint` yalnız IDNA host, scheme ve effective port taşır; raw path/query/user-info/fragment taşımaz.
- Başarılı sonuç username, password veya full locator döndürmez; yalnız random opaque `SecretReference` ya da `ProtectedLocatorReference` içerir. Bunların platform-protected persistence'ı M4 kapsamıdır.
- Source adı 100, locator 4096, username 256 ve password 1024 Unicode scalar ile sınırlıdır; NFC, invalid UTF-16, control/NUL ve IDNA/IPv4/IPv6 vakaları table testlerindedir.
- URI/header/untrusted-text diagnostics policy'si raw input'u geri üretmez. `.m3u8` uzantısı catalog/HLS kararı vermez; bounded content-prefix classifier kullanılır, gerçek incremental parser M7'ye kalır.

## M4 protected-storage foundation — COMPLETED, 2026-08-20

- `IptvSuite.Application`, arbitrary string key taşımayan typed `ISecretStore` portunu; source/purpose/reference binding'ini; dispose sırasında owned buffer'ı sıfırlayan ve JSON/debug çıktısında `[SENSITIVE]` dışında veri vermeyen `SecretLease`i içerir.
- `SourceDraftProtectionService`, Xtream-compatible veya remote-playlist draft'ını store mutation'ından önce validate eder. Bounded, versioned ve length-prefixed strict UTF-8 payload'ı typed create operation'ına verir; store başarısından sonra exact store-issued opaque reference'ı validated draft'a bağlar ve geçici byte buffer'ı `finally` içinde sıfırlar. Doğrudan await edilen çağrıda başarılı store commit'inden sonra cancellation yeniden gözlenmez ve reference başarılı sonuçla döner. Caller abandonment/retry, aynı `SourceId` için yinelenen create, process crash/OOM, gelecekteki DB/configuration persistence veya deletion/reconciliation bu sınırın dışındadır.
- `SourceConfigurationProtectedRecordDeletionService`, yalnız `DeletionPending` durumundaki authoritative `ContentSource` aggregate'inden `SourceId`, `SourceConfigurationId` owner'ı, purpose ve opaque reference'ı türetip Xtream credential ya da remote-playlist locator kaydını idempotent siler. Ayrı key/owner/reference parametresi kabul etmez; pre-cancel store'a ulaşmaz ve başarılı delete commit'inden sonra cancellation'ı yeniden gözlemez. Channel kayıtları, metadata/snapshot/cache, source status persistence, yeni operation admission/drain ve bilinmeyen orphan kayıtları bu dar primitive'in kapsamı değildir.
- `SourceChannelProtectedRecordDeletionService`, caller'ın durable olarak `DeletionPending`e alınmış saydığı `ContentSource` ile authoritative persistence'tan yüklenmesi gereken `PlaylistSnapshot` ve `LiveChannel` aggregate zincirini kabul eder. Source→snapshot, snapshot→channel ve channel stable-key→source ilişkilerinden biri bozuksa store'a ulaşmaz; ayrı source/owner/purpose/reference parametresi açmadan `ChannelId` owner ile varsa exact protected-stream kaydını, ardından optional logo kaydını idempotent siler. İlk başarılı delete commit'inden sonra newly-requested cancellation ikinci exact delete denemesini durdurmaz; stream silinip logo silinememişse aynı çağrının idempotent retry'ı, store yeniden başarılı olduğunda sonuca yakınsamaya izin verir. Bu validation provenance veya authorization kurmaz. Cleanup yalnız active/`Complete` snapshot'a daraltılmaz; authoritative zincirdeki retained, `Importing` veya `Rejected` snapshot kayıtları da silinebilir. Primitive source status'unu persist etmez, admission kapatmaz, in-flight operasyonları drain etmez, bütün channel'ları enumerate etmez, metadata/snapshot/cache ya da orphan kayıtlarını temizlemez ve configuration primitive'iyle birlikte kullanıldığında bile source-wide deletion coordinator sayılmaz. DPAPI user-boundary statik contract'ı dahil exact SDK `10.0.302` local worktree gate'inde architecture 16, unit 154 ve integration 34 olmak üzere 204/204 test iki identical `Passed` setiyle ve tüm fixture/sentinel/scanner/artifact-canary kontrolleriyle geçmiştir; summary `commitSha=null` taşır. Normal full gate gerçek local-user executable'ını çalıştırmaz. Ayrı run number `#21`, commit `0ef0da5793dc4fd1f4e83dbe38cd49d7a7ccb56f` üzerinde 3/3 yeşil iş, üç artifact ve `9m25s` ile hosted execution'ı workflow/UI düzeyinde **VERIFIED** yapar; artifact içeriği ve tam digest bağı bağımsız doğrulanmamıştır.
- `IptvSuite.Infrastructure`, stable `System.Security.Cryptography.ProtectedData 10.0.10` ile yalnız `DataProtectionScope.CurrentUser` kullanan Windows adapter'ını içerir. Bounded v2 binary envelope source, purpose, semantic owner kind/ID, reference kind ve opaque record ID'yi entropy, dosya adı ve korunan içeriğe birlikte bağlar. Source credential/remote playlist için owner `SourceConfigurationId`, channel stream/logo için `ChannelId`dir.
- Yazım aynı dizinde `CreateNew` temp + `WriteThrough` + `Flush(true)` + overwrite rename ile yapılır; yalnız transient Windows access/share/lock kodları bounded retry alır. Raw path, exception mesajı veya secret result'a taşınmaz.
- Windows composition root, packaged launch sırasında factory'yi tam bir kez çağırır; başarılı store'u uygulama ömrü boyunca tutar, initialization başarısızsa pencereyi oluşturmadan fail-closed olur ve fallback store oluşturmaz. Factory, codename'i persistence path'ine koymadan `ApplicationData.LocalCachePath\ProtectedStore\v2` kullanır, beklenen filesystem/path/WinRT initialization hatalarını store veya diagnostic context taşımayan typed `StorageUnavailable` sonucuna eşler ve caller cancellation'ı `OperationCanceledException` olarak korur.
- Adapter başlangıcı yalnız protected-store kökündeki tam lowercase `temporary-v2-{32 hex}.tmp` biçimli, regular ve en az 24 saatlik crash artığını idempotent temizler. Fresh/future, lookalike, nested, v1 namespace ve `.dpapi` kayıtlarına dokunmaz; non-regular/reparse veya silinemeyen exact v2 stale entry'de fail-closed davranır. Cleanup, exact current-namespace aday listesini 1.024 girdide sınırlar; 1.025'inci exact aday görülürse hiçbir adayı silmeden `IOException` ile fail-closed olur. Dizin aramasının toplam maliyeti bu aday cap'iyle sınırlandırılmış sayılmaz; 50k `Decision` koşusu yalnız unpackaged restart probunu ölçmüştür.
- M4 fake contract testleri ve gerçek Windows DPAPI testleri CRUD/update, adapter restart, idempotent delete, pre-cancel, concurrent create, aynı süreçte iki adapter instance'ı arasında same-key update/read/delete sıralaması, ciphertext swap/corruption/oversize, same-source semantic owner/ref-swap reddi, bounded startup temp cleanup ve 1.024/1.025 temp-aday sınırı, zeroization ve canary-at-rest taramasını kapsar. Ayrı Application unit testleri source-draft pre-validation/no-mutation, deterministic bounded encode, exact purpose/owner/reference binding, safe failure mapping ve doğrudan await edilen post-commit cancellation sınırını kapsar. Internal source-payload v1 decoder exact round-trip, magic/kind/version, truncation, length overflow, trailing-data, strict UTF-8/control/whitespace ve Unicode-scalar sınırlarını fail-closed test eder; string/URI üretmez. Public resolve, M5 origin policy, lifecycle gate ve UI wiring henüz yoktur.
- Normal MSTest process'i/temp-root kanıtına ek olarak ayrı `IptvSuite.PackageLifecycleHarness`, aynı disposable signer/package family ve kullanıcı bağlamında baseline `0.0.1.0` create/duplicate-create rejection; normal `Add-AppxPackage` ile `0.0.2.0` update; updated-process read/wrong-owner/update/delete/post-delete; exact updated-package reset + fresh create; canlı protected state ile exact current-user uninstall + app-data yokluğu; aynı updated MSIX reinstall'i + ikinci fresh create/delete zincirini kodlar. Run number `#18`, update zincirinin; run number `#20` ise hardened schema-v3 reset/live-state uninstall/reinstall zincirinin hosted execution'ını sırasıyla commit `bfe5960d5bc911e21ba1e1e1675deb575400e531` ve `6f30e3fa92f329c1fa26333c591edb5148261cec` için workflow/UI düzeyinde **VERIFIED** yapmıştır. Artifact içerikleri bağımsız doğrulanmamıştır. Harness production payload'a girmez. Gerçek farklı-user boundary acceptance'ı M4'te tamamlanmıştır; durable source-wide lifecycle coordinator/reconciliation M8; resolved-origin policy M5; aynı SQLite transaction domain'indeki production katalog yerleşimi ile crash/end-to-end kanıtı M8; production PFN/repair/identity ve Store kabulü M15 kapsamında açık kalır. Protected-catalog comparative benchmark'ı kapanmıştır.
- Ayrı `IptvSuite.DpapiUserBoundaryHarness`, framework-dependent x64 ve nonpackable/nonpublishable test executable'ıdır; production graph'ı ve MSIX payload'ı onu reddeder. Creator process sentetik raw `CurrentUser` blob'u ile gerçek production-adapter kaydını hazırlar. Farklı SID'li non-admin local-user child process raw blob'u açamamalı ve creator kaydı için `ProtectedRecordUnavailable` + null lease almalı; kendi raw/adapter round-trip'i geçmeli ve creator record byte'larını değiştirmemelidir. Creator son aşamada aynı blob/kaydı yeniden okuyup exact cleanup yapar. Controller `CreateProcessWithLogonW(LOGON_WITH_PROFILE)` ve target profile environment'ını kullanır; aynı caller token'ını koruyan `LOGON_NETCREDENTIALS_ONLY` yasaktır [S107–S111]. Sanitized evidence username/SID/path/password/payload/reference/protected-record digest içermez; success evidence exact SDK, clean commit SHA, controller script SHA-256 ve staged harness assembly SHA-256 provenance metadata'sını taşır. Child invocation/custom environment password/payload/reference/SID/protected-record digest taşımaz; explicit custom environment block yoktur, OS target-profile environment username/profile değerlerini sağlar ve exact executable/DLL/workspace path command line'da zorunludur. Kod ve statik gate `IMPLEMENTED`dır. 2026-08-16 run `#31` (`31960438511`), commit `740d33d0d428c8f6aeded24aa358fdd8626016f1` için dört zorunlu Windows işini ve dört artifact'ı `8m23s` içinde geçmiştir. İndirilen 1.766 bayt sanitized DPAPI `last-success.json` SHA-256 `c9dbb22b577aaf08c588bbdc951e87bce6bbe82c86c8f58a2fe6cb9840b725b8` taşır; exact SDK/clean commit/provenance ve bütün boundary/cleanup Boolean'ları `true` olduğundan raw/adapter farklı-user lane'i hosted **VERIFIED**dır. Bu M4 raw-DPAPI/production-adapter user-boundary lane'idir; production package'ın ikinci user registration/PFN/Store identity ve clean-machine kabulü M15'e aittir.
- Managed containment/reparse kontrolleri path tabanlı TOCTOU yarışını bütünüyle kapatmaz; cleanup non-adversarial filesystem yarışlarında fail-closed/best-effort sınırındadır. 2026-08-14 M4 threat-model kararı, production uygulamanın yükseltilmeden çalıştığı ve aynı Windows kullanıcısı altında kötü amaçlı kod yürütülmesinin zaten hesap/oturum compromise'ı sayıldığı sınırda bu yarışı residual integrity/availability riski olarak kabul eder. Executable manifesti `asInvoker` + `uiAccess=false` değerlerini açıkça ister ve architecture gate bunu kilitler; uygulama kendiliğinden elevation istemez [S95]. Bu kabul atomik path güvenliği iddiası değildir: store kökü paylaşılan/ağ konumuna taşınırsa, süreç yükseltilirse veya same-user adversary kapsama alınırsa karar yeniden açılır. Tam kapatma, yalnız son bileşeni açan küçük bir `SafeFileHandle` değişikliği değil; pinned directory handle üzerinden root-relative child open/create, attribute, rename, delete ve enumeration sağlayan ayrı Windows I/O adapter'ı gerektirir [S94]. `.dpapi` source-wide deletion/record reconciliation M8 durable inventory/transaction acceptance'ında açık kalır.

### M4 opt-in secret-store spike

`IptvSuite.SecretStoreSpike`, production payload'a girmeyen ve normal quality/CI tarafından çalıştırılmayan Release x64 ölçüm executable'ıdır. Tamamen sentetik 256-byte locator-benzeri değerleri gerçek CurrentUser DPAPI adapter'ına yazar; ölçüm dışı kısa warmup ardından create, restart, bounded read probe, delete ve cancellation-boundary ölçümlerini aggregate-only evidence olarak `.artifacts/m4-secret-store-spike/evidence` altına kaydeder. Raw payload/reference/path, kullanıcı veya makine adı evidence'a yazılmaz; warmup, measured ve cancellation protected root'ları silinmeden önce, evidence ise izole staging dizininde taranıp yalnız temizse atomik olarak yayımlanır. Cancellation kaydı request→completion latency'yi, iptal anındaki committed alt sınırını, iptal sonrası commit üst sınırını ve completion sonrası yeni mutation olmadığını ayrı alanlarda taşır.

2026-08-14'te clean commit `fc96a211171d1e4f5e5f02174da6c565ef2d59bb` üzerinde 5k/10k/20k/50k × 20 `Decision` matrisi sıfır exit code ile tamamlandı. Local sanitized evidence ve girdi hash bağları **VERIFIED**dır: 50k create p95 `151.447 s`, create allocation p95 `668.007 MiB`; cancellation request→completion p95 `1.067 ms` ve post-cancellation commit üst sınırı `0`dır. `INFERENCE`: per-record DPAPI bulk channel-locator yerleşimi tek başına 50k normalize + protected persistence + index için birleşik ≤3 s süre bütçesini ve bütün-import ≤150 MiB allocation bütçesini aştığı için bu kullanım için reddedilmiştir. Bu sonuç source-credential kararı değildir. Restart ve 256-record read ölçümleri yalnız dar unpackaged problardır; production PFN/`LocalCache`, tam 50k read/restart, parser/normalize/index veya UI kanıtı değildir. Aynı yerleşimi yeniden koşmak yeni gate değildir. Aynı host/protokoldeki protected-catalog comparative `Decision` daha sonra tamamlanmış ve comparative gate'i kapatmıştır; production SQLite transaction yerleşimi ile end-to-end/crash kanıtı M8'de açıktır. Ayrıntı: [M4 secret-store Decision evidence](../../docs/quality/M4_SECRET_STORE_DECISION_EVIDENCE.md) ve [M4 protected-catalog Decision evidence](../../docs/quality/M4_PROTECTED_CATALOG_DECISION_EVIDENCE.md).

Hafif harness kontrolü:

```powershell
.\eng\Invoke-WindowsSecretStoreSpike.ps1 -Mode Smoke
```

`Smoke`, 1.000 kayıt ve tek turdur; performans kabulü veya ADR kararı değildir. `Decision`, sabit 5k/10k/20k/50k matrisini her ölçek için 20 tur ve 20 cancellation örneğiyle çalıştırır. Yoğun ve uzun disk yazımı nedeniyle otomatik başlatılmaz; clean worktree üzerinde bilinçli olarak şu komut gerekir:

```powershell
.\eng\Invoke-WindowsSecretStoreSpike.ps1 -Mode Decision -AllowDecision
```

Evidence dosyasının varlığı tek başına başarılı run anlamına gelmez: başarısız yeni çağrı önceki temiz summary'yi koruyabilir. Yalnız betiğin sıfır exit code'u ile bitmesi ve summary içindeki commit, runner assembly, spec ve workload hash'lerinin değerlendirilen koşuyla eşleşmesi PASS sayılır. Runner assembly hash'i dependency setinin veya reproducible-build eşitliğinin kanıtı değildir.

Bu unpackaged test-host sonucu bile packaged `LocalCache`, gerçek ikinci kullanıcı, source lifecycle, parser/normalize/index maliyeti veya 5 saniyelik uçtan uca import bütçesi değildir. `Decision` sonucu yalnız ADR-003 girdisidir; layout seçimi ayrıca güvenlik/lifecycle incelemesi ister.

### M4 opt-in protected-catalog aday spike'ı

`IptvSuite.ProtectedCatalogSpike`, reddedilen per-record DPAPI bulk layout'ıyla aynı sentetik workload/protokolde karşılaştırılmak üzere ayrı tutulmuş test-only Release x64 executable'dır. Proje nonpackable/nonpublishable'dır; solution build'inde derlenir fakat executable'ı normal quality/hosted workflow tarafından çalıştırılmaz, production project reference'ı alamaz ve production package payload denetimi aday assembly/artifact adlarını denylist ile reddeder. Hafif harness kontrolü repository kökünden şöyle çalıştırılır:

```powershell
.\eng\Invoke-WindowsProtectedCatalogSpike.ps1 -Mode Smoke
```

Prototype source/snapshot başına immutable tek container, fresh 256-bit DEK, DEK için bir CurrentUser DPAPI wrap ve her kayıt için AES-256-GCM kullanır. V1 format big-endian ve footerless'tır. Activation öncesi kontrol structural reopen, DPAPI unwrap ve en fazla 16 eşit aralıklı authentication-tag probe ile sınırlıdır; full scan veya power-loss durability kanıtı değildir. `adapterReopenAndUnwrap` yeni adapter nesnesini aynı process içinde ölçer, cold-process restart sayılmaz. Staging cancellation sayacı committed record değil encrypted staged record'dur; cancellation sonrası staging resume edilmez.

2026-08-14 local dirty-worktree `Smoke`, exact SDK `10.0.302` ile locked restore ve Release x64 build'i 0 warning/error tamamlayıp 1.000 kayıt × 1 tur çalıştırdı. `readerValidation` altındaki 22 Boolean true'dur. Ayrı 50.000 kayıtlı cancellation probu tam 10 encrypted staging kaydından sonra iptal istemi verir; post-cancel encrypted üst sınırı, activation, completion sonrası mutation ve temp artifact sayılarının her biri `0`, canary ve cleanup PASS'tir. Targeted architecture suite `14/14` geçmiştir. Sanitized `.artifacts/m4-protected-catalog-spike/evidence/smoke-summary.json` dosyası `20469` byte ve SHA-256 `9d52172e61416fa1c73206ec745bd72a5aa18f15844153b08026c4597494cb99` değerindedir; exact input hash'leri spec `2693eed7032d4c8ea50a062cd8b123ba782a89293e9d31b079335d3f6aa15666`, lock `fa138ce4203c6967edaeb8031722d72fd8372f1ed08bd9a52dff6342b582e730` ve runner assembly `a7697286871d17a96abe4c238975c57bd5bc1878c70735ead632d963c7989cf3` olarak kaydedilmiştir. Summary repository commit alanında `207455a54d2d7ac9b6b5c1ce8eb5e29bbee0c383`, `isDirty=true` ve `decisionEligible=false` taşır; bu nedenle performans kararı değildir.

Bu hedefli `14/14` kaydını izleyen tarihsel protected-catalog local full gate; exact SDK `10.0.302`, Debug/Release x64 build, architecture 14 + unit 151 + integration 34 = `199/199 × 2`, sentinel fail/recovery, scanner ve artifact-canary PASS sonucu üretmiştir; summary `commitSha=null` taşır. O gate aday projesini solution build'inde derlemiş ve isolation kurallarını test etmiş, fakat aday executable'ını veya ağır `Decision` modunu çalıştırmamıştır. Güncel 201-test DPAPI user-boundary full-gate sonucu dosyanın başında ayrıca kayıtlıdır.

Fresh explicit onayla clean/eligible commit `7cd5bf739712a5cbdcf47634275a113faab8c48e` üzerinde 5k/10k/20k/50k × 20 `Decision` tamamlandı. Bütün raw sample/hash/aggregate kontrolleri, 22 reader-validation Boolean'ı, fault/cancellation matrisi, canary ve cleanup **VERIFIED**dır. 50k p95 değerleri: create+activate `173,128 ms`, allocation `36360200` byte (`34,676 MiB`), working-set boundary delta `16625664` byte (`15,855 MiB`), aynı-process adapter reopen+unwrap `13,231 ms`, 256-record read `1,695 ms`, snapshot delete `2,321 ms`, disk `16800374` byte (`16,022 MiB`) ve staging cancellation `5,570 ms`; post-request encryption/activation/post-completion mutation/temp artifact sayıları `0`dır. Baseline'a göre create `874,766×` hızlı (`%99,886` azalma), allocation `19,264×` düşüktür (`%94,809` azalma). Exact `.artifacts/m4-protected-catalog-spike/evidence/decision-summary.json` `209.828` byte ve SHA-256 `4a219e802dcff959644bda42489f0ee5231be63df1eb37d034e2ed16ff0859de` değerindedir; spec `2693eed7032d4c8ea50a062cd8b123ba782a89293e9d31b079335d3f6aa15666`, license `0ee38448ce47fb7c98e56984a84819138e0f7eec085b03d75607d3f5f1d0dba3`, lock `fa138ce4203c6967edaeb8031722d72fd8372f1ed08bd9a52dff6342b582e730`, runner `293897de526def889b902b2474dbffd924ab4cc80b9c5ca654791578b8dd80cc`, Testing `d3b701edae7112dc5edf4d11409024acdbd0230c2278dec6d06ca5de9663e8ba`, deps `6a8972d894f1dd0604e0f02b820f34678cb9378af3b8e73037f8571118a32267` ve global/baseline workload `eb6a4eaaecf437e80ef01feb00c6d1453e41994682a76ed08f81c1808a372f3f` bağlarını taşır. Runner threshold uygulamaz. Component'in referans bütçelerin altında kalması comparative benchmark gate'ini kapatan **INFERENCE**dır; normalize/index/SQLite/parser/UI/network/packaged lifecycle hariç olduğundan end-to-end PASS değildir. Immutable cross-file container production için **NO-GO**dur. Security-reviewed yön, DPAPI-wrapped per-snapshot DEK + per-row AES-GCM + active snapshot'ı aynı SQLite transaction domain'inde tutmaktır; M8'de `Proposed`/unimplemented kalır. M4 foundation `COMPLETED`, ADR-003 `Proposed`dur; ayrıntı [Decision evidence](../../docs/quality/M4_PROTECTED_CATALOG_DECISION_EVIDENCE.md) ve [aday kaydında](../../docs/quality/M4_PROTECTED_CATALOG_CANDIDATE.md) tutulur.

## M2 iki-run quality gate

Repository kökünde tek quality komutunu çalıştırın:

```powershell
.\eng\Invoke-WindowsQualityGate.ps1
```

`Invoke-WindowsQualityGate.ps1`; `global.json` içindeki exact SDK/`rollForward: disable` kuralını doğrular, locked restore yapar, Debug ve Release x64 build'lerini tek MSBuild node ile çalıştırır, architecture/unit/integration projelerini Release'te ayrı TRX dosyalarıyla iki kez koşar ve iki koşunun sıralanmış `testName|Passed` setlerini karşılaştırır. Tek-node sınırı high-core Windows host'larında restore/build process fan-out'unu sınırlar; method-level paralel test izolasyonu korunur. Her test host'unda iki dakikalık hang timeout vardır; secret taşıyabilecek dump üretilmez. TRX dosyaları timestamp ve execution order nedeniyle byte-identical olmak zorunda değildir.

Betik fixture'ı iki ayrı dizinde üretip `records.json` ile `fixture-manifest.json` SHA-256 değerlerinin eşitliğini denetler. Sonra `Invoke-QualityGateSelfTest.ps1` aracılığıyla `IPTV_SUITE_ARM_QUALITY_GATE_SENTINEL=1` iken hedef testin gerçekten başarısız, değişken kaldırılınca yeniden başarılı olduğunu kanıtlar; process'in önceki environment değerini `finally` içinde geri yükler. Quality artifact'larını summary yazılmadan önce ve sonra canary marker'ı için tarar.

Her çağrı yalnız exact `.artifacts/quality-gates` alt ağacını temizleyip yeniden üretir. TRX'ler `.artifacts/quality-gates/test-results/run-{1,2}`, fixture'lar `.artifacts/quality-gates/fixtures/run-{1,2}`, sanitized özet ise `.artifacts/quality-gates/evidence/quality-summary.json` altında kalır. Bu dosyaların varlığı tek başına PASS değildir; komutun sıfır exit code ile tamamlanması gerekir.

### Test katmanı sınırları

| Katman | M2'de kanıtladığı | Kanıtlamadığı |
|---|---|---|
| Unit | Fake time advance, scripted transport, in-memory fake secret store, passive fake player, fixture/canary helper | Gerçek timer/network, DPAPI, codec veya ürün state policy'si |
| Integration | Loopback Kestrel, OS-seçimli port, timeout/isolation; M4 fake contract ve normal Windows host'ta gerçek CurrentUser DPAPI | Gerçek provider/internet/TLS, packaged LocalCache lifecycle, second-user, database veya Store lifecycle |
| Architecture | Production/test project ve package/framework reference allowlist'i | Runtime davranışı |
| Packaged-host smoke | Signed development MSIX install, AUMID launch, görünür shell, basic catalog UIA name/role + Tab focus sırası, normal close ve exact cleanup | Data-bearing browse, realized-container/frame, Narrator/genel accessibility, provider/player/codec, update veya Store kabulü |
| Packaged lifecycle | Ayrı test MSIX'inde baseline `0.0.1.0` create/duplicate-create, same-family/signer `0.0.2.0` normal update, updated-process CurrentUser DPAPI v2 read/wrong-owner/update/delete/post-delete; schema v3'te exact reset + fresh create ve live-state uninstall + aynı updated MSIX reinstall + fresh create/delete; update bölümü run number `#18`, hardened schema-v3 bölümü run number `#20` ile hosted workflow/UI düzeyinde **VERIFIED** | İndirilen artifact alanlarının bağımsız doğrulaması, production PFN, repair, second-user, crash/power-loss veya source-wide reconciliation |

`IptvSuite.Testing`, `IptvSuite.UnitTests`, `IptvSuite.IntegrationTests`, opt-in `IptvSuite.SecretStoreSpike`, opt-in `IptvSuite.ProtectedCatalogSpike`, packaged `IptvSuite.PackageLifecycleHarness`, unpackaged `IptvSuite.DpapiUserBoundaryHarness`, M9 `IptvSuite.CatalogUiAcceptanceHarness`, rejected-candidate `IptvSuite.PlaybackCompatibilitySpike` ve ADR-007 `IptvSuite.NativePlaybackCompatibilitySpike` production graph'ına girmez. M9 harness'ı yalnız disposable package LocalCache'e 50k sentetik provider-key channel seed eder. M10 candidate scaffold'ı exact LibVLC setinin NO-GO kararını tekrar üretmek için korunur; native fallback spike'ı yalnız Microsoft `MediaPlayer`/Media Foundation surface ve loopback sentetik corpus kullanır. Harness'lar nonpackable/nonpublishable kalır ve production MSIX payload denylist'i exact assembly adlarını reddeder. Protected-catalog adayı yalnız `IptvSuite.Testing` ile mevcut `ProtectedData` paketine bağlı, nonpackable/nonpublishable x64 test executable'ıdır. DPAPI user-boundary harness'ı Domain/Application/Infrastructure/Testing ve doğrudan mevcut `ProtectedData` paketine bağlı framework-dependent x64 test executable'ıdır; solution build'inde derlenir fakat deploy edilmez. M2 fake secret store/player production port'u değildir; IntegrationTests içindeki M4 fake yalnız gerçek `ISecretStore` contract senaryosudur ve DPAPI/package kanıtı yerine geçmez.

## Sentetik fixture ve canary

ADR-007 Tier A playback corpus'u `tests/fixtures/playback/tier-a` altındadır. `eng/New-WindowsTierAPlaybackCorpus.ps1`, ffmpeg.org'un bağladığı exact SHA-256'lı BtbN generator archive'iyle yalnız `lavfi testsrc2` + `sine` kaynaklarından direct H.264/AAC MPEG-TS ve relative HLS-TS üretir. HLS playlist exact `#EXT-X-VERSION:6`, bir `#EXT-X-INDEPENDENT-SEGMENTS` ve sıralı dört local segment taşır; pinned generator her segmentin ilk video paketini keyframe, ilk access unit'ini SPS/PPS/IDR olarak doğrular. Runtime controller tag tekilliği/sırası ve URI sırasını authenticated manifestten sonra yeniden denetler [S126–S128]. Direct TS ile dört segmentin byte/hash'leri korunmuş, yalnız playlist ve ona bağlı LF-normalized manifest değişmiştir. Manifest CC0/provenance, codec tuple, boyut ve file hash'lerini taşır; generator executable/library'leri repository veya MSIX'e girmez. Bu standards-correct fixture hardening'inin varlığı performans veya playback acceptance sonucu değildir.

`VERIFIED — supply-chain rebaseline, 2026-08-22`: Önceki central `Microsoft.WindowsAppSDK 2.3.1`, transitive `Microsoft.WindowsAppSDK.WinUI 2.3.0` çözüyor; exact paketteki Engineering Preview lisansı live-environment ve dağıtım kısıtları taşıyordu. Microsoft #6654 kaydı bu sürümü etkilenen set içinde gösterir ve yayımlamadan önce Windows App SDK `2.4.0 → WinUI 2.3.6` düzeltmesine geçilmesini ister [S121][S122]. Resmî NuGet'ten indirilen yeni root/WinUI paket SHA-256 değerleri sırasıyla `6ec2ebb6add33ecebac1f5773ad4cabe934b82fb18d7bea98e011bb0fc0a37b9` ve `3404f14b5aad656024cf23efab49373e14be4129b1e7d8cdd18dd26712d3c8e2`; byte-identical `license.txt` SHA-256 değeri `5b11e6347756e40fe0274bc08c97f89201b94f0d50181a09a00f1f4740840501`dir. İmzalar doğrulanmış, yeni license Engineering Preview/live-use yasağı taşımamıştır. Bu değişiklik exact MSIX/runtime binary setini değiştirdiğinden önceki M10 playback/package/soak kayıtlarının tamamı yeni graph için tarihsel kanıttır ve güncel acceptance yerine kullanılamaz.

Fail-closed gerçek signed-MSIX inventory validator'ı run `32557484626` içinde çalışıp geçti; native budget failure'ı nedeniyle kalıcı sanitized success artifact'ı yayımlanmadı. Teknik inventory geçişi hukuk kabulü değildir; repository kök `LICENSE`/`NOTICE`, uygulama asset provenance'i, corpus sahipliği/CC0 dağıtım hakkı, codec/patent uzman incelemesi, WACK, gerçek cihaz/HW-decode ve kalan soak/surface/lifecycle matrisi açıktır. ADR-007 `Proposed`, M10 `IN PROGRESS` ve R15 `ACTIVE` kalır; M11 başlamaz.

ADR-007 native fallback acceptance controller'ı elevated, interactive Windows Client üzerinde disposable signed MSIX'i kurar; yalnız TLS loopback sentetik Tier A corpus'unu kullanır ve exact package/certificate cleanup uygular. Kısa hosted diagnostic varsayılan 25 alternating switch'tir. Authoritative long-soak komutu tam 100 switch, ilk switch'ten başlayan 8 saat süre, 5 dakikalık resource örnekleri ve 30 dakika warm-up sonrası `%10` + `100 MiB` memory sınırını fail-closed uygular:

Schema-9 cleanup sahiplik sınırında koşu öncesindeki `Microsoft.WindowsAppRuntime.2` exact package-full-name baseline'ının tamamı koşu sonunda varlığını korumalıdır. Güncel `2.4.0` successor graph'ında sonradan görülen kayıtlar yalnız exact Microsoft publisher/family, framework, `>=2.4.0.0` version ve izinli X64/X86 architecture doğrulamasını geçebilir; X86 kayıt aynı version'da exact X64 sibling ister. Bu shared framework ekleri korunur ve hiçbir zaman `Remove-AppxPackage` hedefi yapılmaz; exact disposable test MSIX'i, package-family app data'sı, process'i, koşu sertifikaları ve output'u için cleanup zorunluluğu değişmez [S116]. Sanitized evidence bu ayrımı `RuntimePackageBaselinePreserved=true`, `RuntimePackageGraphDisposition=ExactRestored|SharedAdditionsPreserved` ve bounded `RuntimePackageSharedAdditionCount` ile taşır. `2.3.1` graph'ındaki önceki schema-9 sonuçları tarihsel kalır.

`VERIFIED`: Run `32537541013` ilk attempt'i, beş HLS yanıtının ve `940229` byte'ın eksiksiz teslimine rağmen ilk `MediaOpened` için `MediaOpenTimeout` verdi. Tek bounded rerun 100 switch'i ve schema-9 cleanup yolunu tamamladı; fakat startup p95 `2157,0052 ms` iken maximum `5056,3179 ms` ile değişmeyen 5 saniye tavanını geçti. İlk `Source`/`Play` çağrısından önce bounded `MediaPlayerElement.Loaded` bariyeri ekleyen `55945b9` checkpoint'inin hosted run'ı `32540016736`, readiness sınırını geçtiği halde aynı ilk-HLS `MediaOpenTimeout` sonucunu verdi; beş yanıt/`940229` byte eksiksiz, transport failure sıfır ve installation type `Server` idi. Böylece eksik `Loaded` sınırı tek başına neden olarak reddedildi. Statik inceleme sonrası eklenen `MediaPlayer.RealTimePlayback=true` checkpoint'inin run'ı `32540817711`, 100 switch probe invariant'larını tamamladı ve p95 `2162,9392 ms` ölçtü; ancak maximum `5102,2433 ms` ile hard tavanı `102,2433 ms` aştı. HLS/direct p95 `2165,6151/2162,2629 ms` olduğundan real-time konfigürasyonu hosted root-cause düzeltmesi sayılmaz. Tamamlanmış-maximum attribution'lı commit `12fac08` için run `32542190663`, quality/package/DPAPI işlerini geçti; native iş beş HLS response/`940229` byte ve sıfır transport failure sonrasında ilk `MediaOpenTimeout` ile durdu. Envelope v2 tamamlanmamış örneği ölçmediği için bütün maximum diagnostic alanları neutral kaldı. Probe envelope v3, maximum alanlarını değiştirmeden reset/dispose öncesi active stage/switch/fixture/attempt/surface ve source-create/source-assign/`Play`/active-stage sürelerini exact doğrulayıp failure loguna bağlar. Kalıcı schema-9 success şekli, startup bütçeleri, HLS-first sırası ve timeout retry yasağı değişmez. [Attribution kaydındaki](../../docs/quality/M10_NATIVE_TIER_A_STARTUP_ATTRIBUTION.md) schema-3 sonucu aşağıdaki #148 gözlemiyle tamamlanmıştır.

`VERIFIED`: Envelope-v3 commit `c299410` için [run `32543488245`](https://github.com/serkankaracan/iptv-suite/actions/runs/32543488245), 100 switch'i retry olmadan tamamladı; p95 `2150,7287 ms` iken ilk HLS/attempt-1 maximum'u `5049,0157 ms` oldu. Bunun `165,1331 ms` pre-wait + `4883,8826 ms` `MediaOpened` wait ayrımı ve direct maximum `2302,9441 ms`, dominant sürenin `MediaOpenWait` stage'inde olduğunu doğruladı; bu stage normal loopback teslimi ile media pipeline'ı ayırmaz ve hard tavan `49,0157 ms` aşıldığından koşu PASS değildir. Sonraki dar hipotez, `Loaded` sonrasında tek global `CompositionTarget.Rendered` frame'ini ayrı bounded surface precondition olarak bekler. Bu event medya/HLS readiness garantisi değildir [S117, S119]; per-switch source-to-open kronometresi, bütçeler, HLS-first ve retry yasağı değişmez. Etki successor hosted run'a kadar `UNVERIFIED`dır.

`VERIFIED`: Bu hipotezi sınayan commit `1411472` için [run `32544835406`](https://github.com/serkankaracan/iptv-suite/actions/runs/32544835406), quality/package/DPAPI işlerini geçti; native iş global rendered-frame sınırını geçtiği halde ilk HLS/attempt-1'de `MediaOpenTimeout` verdi. `5/5` loopback yanıtı ve `940229` byte tamamlandı, fakat toplam `5154,3957 ms` içinde aktif `MediaOpenWait` `5003,0138 ms` oldu. Dolayısıyla ilk-frame hipotezi reddedildi ve global handler kaldırıldı. Probe envelope v4, mevcut tek 5 saniyelik deadline'ı ve `MediaSource.CreateFromUri` davranışını değiştirmeden attempt-owned `MediaSource.OpenOperationCompleted` zamanını/hata-varlığı bitini kaydeder; handler Source atamasından önce bağlanır ve reset/dispose öncesinde ayrılır [S120]. Kalıcı schema-9 success şekli ve kabul bütçeleri değişmez.

`VERIFIED — envelope-v6 classification`: Commit `78ff98e` için run `32557484626`, quality/package/DPAPI/inventory adımlarını ve 100 switch'i tamamladı. Timeout oluşmadı; ilk HLS total maximum `5024,5709 ms` ile hard tavanı `24,5709 ms` aştı (`p95=2160,3704 ms`, pre-wait/media-open wait `86,1858/4938,3851 ms`, source-open `2454,8193 ms`, direct maximum `2286,6558 ms`, retry `0`). V6 timeout-only alanları neutral kaldı; bu lost-event yarışı veya PASS değildir. Independent-segment fixture checkpoint'inin hosted etkisi henüz `UNVERIFIED`dır.

```powershell
.\eng\Invoke-WindowsNativePlaybackSmoke.ps1 -Configuration Release -SwitchCount 100 -SoakMinutes 480 -NetworkInterruptionCount 7
```

Bu komutun, schema-9 sözleşmesinin veya kodunun varlığı acceptance ya da hosted PASS değildir. Sonuç ancak gerçek Windows Client/reference-device run'ında versioned sanitized evidence, sıfır crash/deadlock, startup bütçesi ve cleanup birlikte geçtiğinde doğrulanır; GitHub-hosted Windows Server sonucu reference-device yerine geçmez ve 8 saatlik acceptance soak henüz çalıştırılmamıştır.

2026-08-21 schema-7 kısa Windows Client checkpoint'i, `Source = null` sonrasında `PlaybackSession.PlaybackState` okumasının `COM E_FAIL (0x80004005)` verdiğini doğruladı. Teardown contract'ı bu geçersiz post-detach sorgu yerine `CanPause` guard → exact `MediaPlayer.Source is null` doğrulaması → attempt-owned `MediaSource.Dispose()` sırasını kullanır. Gerçek packaged koşu 100/100 switch ve 100/100 source detachment; detach p95 `7,986 ms`, maksimum `9,493 ms`; startup p95 `261,169 ms`, maksimum `2.576,309 ms`; altı surface transition; sıfır playback retry; tek sentetik network interruption/recovery (`54→56`) ve exact package/certificate cleanup ile geçti. [Kalıcı session-lifecycle kaydı](../../docs/quality/M10_NATIVE_TIER_A_SESSION_LIFECYCLE_EVIDENCE.md) yalnız source ownership/detachment alt kümesini **PARTIAL VERIFIED** yapar; OS session quiescence/ghost-audio yokluğu, 8 saat, WACK, device/HW-decode, multi-monitor/display-change, audio-device ve sleep/resume kabulleri açık kalır. ADR-007 `Proposed`, M10 `IN PROGRESS` ve R15 `ACTIVE` durumları değişmez.

Schema-10/envelope-v5 cancellation/recovery short lane'i yalnız `SwitchCount=100`, `SoakMinutes=0`, `NetworkInterruptionCount=1`, `CancellationProbeCount=1` profiliyle çalışır. Network `503` fault'u ile managed cancellation birbirinden bağımsızdır. Cancellation exact source detach/dispose ve hedef 1000 ms null-source/no-auto-restart observation; recovery fresh source-specific `OpenOperationCompleted` ve exact current-source position advance ister. Commit `29bb89ce5a2c411f000c5678f4a4eb482e6a9a61` için [run #151 (`32549714175`)](https://github.com/serkankaracan/iptv-suite/actions/runs/32549714175), bütün beş işi yeşil; `100` switch, network/recovery `1/1`, cancellation/recovery count'ları `1`, retry `0`, detach `102`, observation `1000 ms` ve üç Boolean `true` ile geçirdi. [Kalıcı kanıt kaydı](../../docs/quality/M10_NATIVE_TIER_A_CANCELLATION_RECOVERY_EVIDENCE.md) artifact/JSON digest'lerini ve exact metrikleri taşır. Bu native API cancellation, OS audio/session quiescence, M13 reconnect veya M10 completion kanıtı değildir; milestone/risk durumları değişmez.

`apps/windows/testdata/m2/fixture-spec.json`; generator `1.0.0`, algorithm version `1`, seed `20260809` ve 16 tamamen sentetik record tanımlar. Gerçek provider response'u, kullanıcı verisi, credential, playlist veya medya içermez. Aynı girdiden `records.json` ve `fixture-manifest.json` üretmek için:

```powershell
$fixtureRoot = Join-Path ".artifacts\fixtures" ([Guid]::NewGuid().ToString("N"))
$fixtureOutput = Join-Path $fixtureRoot "run-1"
$fixtureLicenseRoot = Join-Path $fixtureRoot "LICENSES"
New-Item -ItemType Directory -Path $fixtureLicenseRoot -Force | Out-Null
Copy-Item .\apps\windows\testdata\LICENSES\LicenseRef-IPTVSuite-Synthetic-Test-Only.txt $fixtureLicenseRoot
dotnet run --project .\apps\windows\tests\IptvSuite.Testing\IptvSuite.Testing.csproj `
    -c Release --no-build -- generate-fixtures `
    .\apps\windows\testdata\m2\fixture-spec.json $fixtureOutput
```

Manifest generator/version/algorithm/seed, file length/SHA-256, provenance flag'leri ve `LicenseRef-IPTVSuite-Synthetic-Test-Only` kaydını taşır. Lisans durumu bilerek `UNVERIFIED` ve internal-only'dir; public redistribution hakkı veya onaylanmış açık kaynak lisansı değildir. Generated output build artifact'ıdır ve commit edilmez.

Canary, `IPTVSUITE_TEST_ONLY_CANARY_V1` tabanlı açık bir test marker'ıdır; gerçek secret değildir. Scanner bilinen marker'ın UTF-8, UTF-16LE, UTF-16BE, URI-escaped ve alignment-safe Base64 biçimlerini scope'tan bağımsız arar; canary taşıyan path'i redakte eder, reparse point'te fail-closed davranır ve pozitif/negatif corpus ile CLI exit `2`/`0` zincirinde test edilir. Bu helper genel credential keşfi, redaction doğruluğu, production log güvenliği veya memory-dump temizliği kanıtı değildir. GitHub job logları artifact scanner kapsamı dışındadır ve canary açısından `UNVERIFIED` kalır; log'a canary yazmama kuralı ayrıca korunur.

## M4 gerçek Windows-user DPAPI boundary smoke

`IptvSuite.DpapiUserBoundaryHarness` production payload'dan ayrı, framework-dependent x64 test executable'ıdır. Controller clean worktree, exact SDK `10.0.302`, 64-bit Windows PowerShell 5.1 ve elevated token ister; random 32 karakterli password ile tek exact local test account oluşturur, built-in Users group'unu numeric SID ile çözer ve hesabın Administrators üyesi olmadığını denetler. Harness output ağacını exact ProgramData test root'una kopyalayıp staged DLL'i exact doğrulanmış `dotnet` executable'ı ile başlatır. Target child `CreateProcessWithLogonW(LOGON_WITH_PROFILE)` altında, null explicit environment block ile target profile environment'ını kullanır; `LOGON_NETCREDENTIALS_ONLY` caller token'ını koruduğu için kodda ayrıca fail-closed yasaktır [S107–S111].

Bu akış gerçek local account/profile yaratıp sildiğinden yalnız bilinçli olarak yönetici açılmış Windows PowerShell'de çalıştırılmalıdır:

```powershell
.\eng\Invoke-WindowsDpapiUserBoundarySmoke.ps1 -Configuration Release
```

Creator phase raw `CurrentUser` blob'u ve gerçek `DpapiCurrentUserSecretStore` kaydı hazırlar. Farklı SID'li non-admin probe; creator raw blob'unu yalnız kriptografik ret ile başarısız sayar, creator production record'u için `ProtectedRecordUnavailable` + null lease ister, kendi raw/adapter round-trip'ini tamamlar ve creator record'u değiştirmez. Creator verification aynı raw/store record'unu digest ve payload düzeyinde yeniden doğrulayıp siler. Success evidence `.artifacts/dpapi-user-boundary/last-success.json` altında exact schema/configuration/platform/scope metadata'sı, exact SDK, clean commit SHA, controller script SHA-256, staged harness assembly SHA-256 ve reviewed Boolean sonuçlarını taşır; account/SID/password/path/payload/reference veya protected-record digest içermez. Failure evidence yalnız stable `Stage` + `Code` alanlarından oluşur. Child process/group membership/account/profile/run+tool workspace cleanup'larından biri başarısızsa success evidence yayımlanmaz. Bu çalışmada komut yürütülmedi ve local OS user oluşturulmadı; hosted workflow execution **UNVERIFIED**dır. Lane yalnız M4 raw/production-adapter user boundary'sidir, ikinci-user packaged PFN/Store identity acceptance M15'e aittir.

## M4 packaged process-lifecycle smoke

`IptvSuite.PackageLifecycleHarness`; ayrı disposable identity kullanan, publish edilemeyen ve uygulama listesinde görünmeyen test-only MSIX hostudur. Production `IptvSuite.Windows` başlangıç yoluna test argument'i, environment switch'i veya hidden mode eklemez. Yönetici PowerShell'de:

```powershell
.\eng\Invoke-WindowsPackageLifecycleSmoke.ps1 -Configuration Release
```

Betik aynı disposable identity/publisher ve geçici signer ile iki exact x64 MSIX üretir: baseline `0.0.1.0` ve yalnız package version'ı farklı update `0.0.2.0`. İlk baseline AUMID activation sentetik canary byte'larını gerçek CurrentUser DPAPI v2 kaydına ve DPAPI-protected control ticket'a yazar; ikinci baseline process aynı ticket varken duplicate create'i fail-closed reddeder. Daha yüksek sürüm normal `Add-AppxPackage -Path ... -DependencyPath ...` yoluyla, force/downgrade veya dependency-only `-Update` switch'i olmadan aynı package family üzerine kurulur. Updated package'ın üçüncü process'i ticket'tan yalnız typed source/owner/reference bağlamını alıp baseline kaydını yeniden okur; yanlış owner read reddini ve yanlış-owner delete'in doğru kaydı koruduğunu, ardından doğru-owner update/read/delete ve post-delete unavailable sonucunu doğrular. Ardından updated package fresh bir protected record/ticket üretir; `Reset-AppxPackage` exact package full name ile çalıştırılır, version/family/full-name kimliği korunurken exact owned state'in herhangi bir manual cleanup öncesinde kaybolduğu doğrulanır ve fresh create'in farklı record identity üretmesi istenir. Bu yeni state canlıyken exact current-user package kaldırılır; preserve-data veya all-users yolu kullanılmadan registration ve package-family app data yokluğu doğrulanır. Aynı signed `0.0.2.0` MSIX/dependency yeniden kurulduğunda package kimliği korunmalı, başlangıç state'i boş olmalı ve yeni create bir kez daha farklı record identity üretmelidir [S91][S92]. Launch'lar yalnız fixed phase ile secretsiz 32-hex run ID alır. Canary gate create, post-update, reset/reinstall fresh-create ve final aşamalarında yalnız harness'ın yazdığı exact `ProtectedStore` ile run-directory ağaçlarını tarar; Windows-managed mutable package hive'ları bu kanıta dahil değildir. Scanner finding, operasyonel tarama hatası ve CLI contract sapmasını ayrı stable code'larla fail-closed raporlar. Evidence schema v3 iki package file/version/SHA-256/signature alanını; update sonuçlarına ek olarak `PackageReset`, `PackageIdentityPreservedAfterReset`, `ResetOwnedStateRemoved`, `FreshCreateAfterReset`, `ResetRecordIdentityChanged`, `PackageUninstalledWithOwnedState`, `UninstallAppDataRemoved`, `PackageReinstalled`, `PackageIdentityPreservedAfterReinstall`, `FreshCreateAfterReinstall` ve `ReinstallRecordIdentityChanged` Boolean'larını allowlist'ler. Test package/output/certificate ve package-family app data exact cleanup sonrasında yalnız `.artifacts/package-lifecycle/last-success.json` sanitized evidence olarak kalır; test MSIX upload edilmez.

Microsoft'un update kısıtları aynı package family ve normal akışta daha yüksek hedef sürüm gerektirir; `Add-AppxPackage` imzalı paketi kullanıcıya `-Path` ile ekler/günceller ve gereken dependency paketlerini `-DependencyPath` ile alır [S89][S90]. Reset app data'yı kalıcı silip uygulamayı fresh-install state'ine döndürür; repair veriyi koruyan ayrı bir davranıştır. Clean uninstall package state'i kaldırır [S91][S92]. Güncel lane exact `0.0.1.0 → 0.0.2.0` test-family update'ine schema v3 reset ve live-state uninstall/reinstall fresh-state zincirini ekler. 2026-08-14 GitHub Actions UI kaydı, run number `#18`in commit `bfe5960d5bc911e21ba1e1e1675deb575400e531` için üç işi yeşil, artifact sayısını üç ve toplam süreyi `9m16s` gösterir; commit-bound workflow'da lifecycle betiği package işinin zorunlu adımı olduğundan yalnız o committeki update execution sonucu workflow/UI düzeyinde **VERIFIED**dır. Aynı tarihli run number `#20` UI kaydı, reset sınıflandırma/retry hardening'ini içeren commit `6f30e3fa92f329c1fa26333c591edb5148261cec` için 3/3 işi yeşil, üç artifact ve `8m14s` toplam süre gösterir. Zorunlu lifecycle adımı reset, post-reset state/fresh-create, live-state uninstall/app-data yokluğu, reinstall ve ikinci fresh create/delete tamamlanmadan başarıya ulaşmadığından schema-v3 zincirinin hosted execution'ı workflow/UI düzeyinde **VERIFIED**dır. Artifact'lar indirilmediği için schema-v3 JSON alanları, ZIP içeriği ve tam hash bağı bağımsız doğrulanmamıştır. Bu geçiş yalnız disposable test PFN, aynı Windows hesabı/runner ve non-preserving current-user kaldırma için kanıttır; production PFN, repair, gerçek ikinci kullanıcı, source-wide deletion/reconciliation, M8 production SQLite transaction yerleşimi, power-loss/crash recovery veya cross-process simultaneous writer kanıtı değildir. Local oturum elevated olmadığı sürece install/lifecycle çalıştırılmaz; hosted Windows job sonucu commit-bound acceptance kaydıdır.

2026-08-14 run number `#19`, commit `71dbb0cf23467d978674976796e2bbceca01494a` için quality job'unu geçirmiş, fakat packaged lifecycle'ı broad `PackageReset`/`PackageResetFailed` noktasında kırmış; required coordinator bunun sonucu olarak fail olmuştur. Bu historical stage hem `Reset-AppxPackage` çağrısını hem hemen sonraki registration/identity/manifest doğrulamalarını kapsadığından exact failing operation ve HRESULT bilinmez; `#19` native reset veya uninstall/reinstall PASS kanıtı değildir. Takip implementation'ı invocation, registration ve manifest postcondition'larını ayrı stage/code'lara böler; en fazla sekiz exception/inner-exception üzerinden yalnız bilinen HRESULT'ları stable, secretsiz failure `Code`larına sınıflandırır ve raw diagnostic yayımlamaz. Yalnız `0x80073D00` (package updating), `0x80073D02` (packages in use) ve `0x80073D05` (app-data deletion) 500 ms/1500 ms beklemeli toplam en fazla üç deneme alır; her deneme öncesi exact harness process için 5 saniye içinde üç ardışık 250 ms absence gözlemi gerekir. Registration readiness ayrıca 15 saniye/250 ms bounded beklenir [S93]. Bu retry politikası resmi hata anlamlarından türetilen `INFERENCE`dır; run `#20` UI'ı retry'ın gerçekten kullanılıp kullanılmadığını göstermez. Bununla birlikte `#20`, takip commit'i `6f30e3fa92f329c1fa26333c591edb5148261cec` için 3/3 yeşil iş, üç artifact ve `8m14s` ile hardening'in ve tüm schema-v3 zincirinin hosted execution'ını workflow/UI düzeyinde **VERIFIED** yapar. Artifact içeriği bağımsız doğrulanmamıştır; `#19` tarihsel failure ve M4 foundation `COMPLETED` kalır.

## Signed packaged-host MSIX smoke

Önce restore ve Release build'i tamamlayın. Ardından repository kökünde **yönetici olarak açılmış PowerShell** içinde:

```powershell
.\eng\Invoke-WindowsPackageSmoke.ps1 -Configuration Release
```

Script şu kontrollü akışı uygular:

1. Source manifestte disposable identity ve yalnız `runFullTrust` capability'sini doğrular.
2. Private key'i export edilemeyen, yedi günlük bir local code-signing certificate oluşturur.
3. Yalnız public certificate'i geçici olarak `LocalMachine\TrustedPeople` deposuna alır.
4. Framework-dependent, signed x64 MSIX ve x64 Windows App Runtime dependency layout'unu üretir.
5. İmzayı, built/installed manifesti, x64 mimarisini ve runtime `2.4.0` dependency'sini doğrular.
6. MSIX'i sınırlı bir temp dizine açıp test/fixture dizinlerini, `IptvSuite.ProtectedCatalogSpike` dahil test assembly/artifact adlarını, fixture manifestini ve bilinen canary marker biçimlerini production payload'ında yasaklar.
7. Paketi kurar, 50k sentetik provider-key katalog seed'ini exact package-family `LocalCache\Catalog\v2\catalog.db` yolunda üretir ve AUMID ile görünür pencereyi açar. Gerçek WinUI ağacında catalog name/role, Tab sırası, 50k status, ≤300 realized automation item ve 20 input-call p95 sınırını denetler; 240 PageUp/PageDown girdili kontrollü foreground scroll sırasında DWM NULL-HWND sistem-compositor proxy'siyle frame p95 ≤33,3 ms, late/drop proxy <%1 ve maximum interval ≤200 ms ister [S113]. Bu app-specific ETW/UI-thread attribution değildir. Exact package family altındaki güncel `LocalCache\ProtectedStore\v2` dizininin oluştuğunu ve reparse point olmadığını doğrular; uygulamayı normal window-close mesajıyla kapatır.
8. Exact development package'ı, package app-data'yı ve exact-thumbprint sertifikayı temizler.

Başarılı kanıt `.artifacts/msix-smoke/last-success.json` altında kalır; schema `ProtectedStoreDirectoryInitialized=true` alanını da taşır. Bütün `.artifacts` içeriği Git dışında build çıktısıdır. Shared `Microsoft.WindowsAppRuntime.2` framework paketi başka uygulamalarca kullanılabileceği için kaldırılmaz. Run `31735655363` içindeki sanitized artifact bu alanı commit `6ac235f49ffd297196189e5e9441ad0c9e04f116` ve o committeki tarihsel v1 namespace için doğrulamıştır. Run number `#15`in yeşil package işi, commit `6da2fc92a223c23d07f7a902de7607e16b1cbb8a` üzerinde güncel owner-bound v2 directory kontrolünü workflow/UI düzeyinde doğrulamıştır; indirilen JSON alanı veya artifact hash'i ayrıca incelenmemiştir. Eski yeşil M2 run'ı packaged store initialization kanıtı sayılmaz.

Bu smoke yalnız çalıştırıldığı elevated, interactive Windows host için package/sign/payload-inspection/install/launch, sentetik data-bearing catalog UIA name/role ve source→category→search Tab sırası, bounded realized-container/input/compositor proxy ölçümü, normal close/uninstall zincirini kanıtlar. Payload kontrolü hedefli test-artifact/canary yasağıdır; genel credential veya malware taraması değildir. Clean VM, non-admin sideload, package update/migration, WACK/Store submission, Narrator/genel accessibility, gerçek provider/internet, player/codec/HW decode, ARM64 veya başka OS/GPU sonucu kanıtlamaz. Run `32443355378`, commit `2ceaffddb8752dd2163a007d8ea458aaea70cc74` için M9 packaged acceptance'ı `VERIFIED` yapmıştır; exact metrik ve artifact hashleri [M9 completion evidence](../../docs/quality/M9_COMPLETION_EVIDENCE.md) belgesindedir.

### Hosted workflow ve kanıt sınırı

`.github/workflows/windows-quality.yml`, her pull request'te, merge queue grubunda, `main` push'unda ve elle tetiklemede GitHub-hosted `windows-2025-vs2026` üzerinde çalışacak şekilde tanımlıdır. Exact SDK `10.0.302` kurulduktan sonra önce `Invoke-WindowsQualityGate.ps1`; yalnız bu job başarılıysa package-smoke/lifecycle işi ile ayrı `Invoke-WindowsDpapiUserBoundarySmoke.ps1` işi paralel çalışır. Raw TRX veya test MSIX upload edilmez; canary-taranmış minimal quality summary, fixture manifesti + license sidecar'ı, packaged-smoke/lifecycle `last-success.json` ve sanitized DPAPI user-boundary `last-success.json` artifact'ları yedi gün tutulur. `Required Windows gate`, quality, package veya DPAPI user-boundary işlerinden birinin skip/fail durumunu tek coordinator sonucunda kırmızıya çevirir. Gerçek-user işi repository/workflow düzeyinde `IMPLEMENTED`; 2026-08-16 run `#31` (`31960438511`), commit `740d33d0d428c8f6aeded24aa358fdd8626016f1` için dört zorunlu Windows işini ve dört artifact'ı `8m23s` içinde geçmiştir. İndirilen 1.766 bayt sanitized DPAPI `last-success.json` SHA-256 `c9dbb22b577aaf08c588bbdc951e87bce6bbe82c86c8f58a2fe6cb9840b725b8` taşır; exact SDK/clean commit/provenance ve bütün boundary/cleanup Boolean'ları `true` olduğundan raw/adapter farklı-user lane'i hosted **VERIFIED**dır. 2026-08-14 UI kaydında run number `#15`, commit `6da2fc92a223c23d07f7a902de7607e16b1cbb8a` için `Locked build and test gate`, `Packaged install and launch smoke` ve `Required Windows gate` işlerinin üçünü de yeşil ve artifact sayısını üç gösterir. Bu workflow/UI kanıtı ilk owner-v2 package ve same-version lifecycle lane geçişini **VERIFIED** yapar. Takip eden run number `#16` UI kaydı, exact deletion primitive'ini içeren commit `3e806edd9a9482b87cccbf000ac8a81823562f6a` için aynı 3/3 işi yeşil, artifact sayısını üç ve toplam süreyi `8m24s` gösterir; bu yalnız workflow/UI-level **VERIFIED** sonuçtur. Run number `#18` UI kaydı ise signed two-version update lane'ini ve package output-directory düzeltmesini içeren commit `bfe5960d5bc911e21ba1e1e1675deb575400e531` için 3/3 işi yeşil, artifact sayısını üç ve toplam süreyi `9m16s` gösterir; commit-bound package job'u lifecycle betiğini zorunlu çalıştırdığı için update lane hosted execution'ı workflow/UI düzeyinde **VERIFIED**dır. Bu tarihsel üç-job UI kayıtları yeni DPAPI işi için execution kanıtı değildir; GitHub database run ID'si, job log/test sayısı, indirilen JSON field seti ve artifact ZIP/içerik/tam hash bağı doğrulanmış değildir. 2026-08-09 repository preflight'ında mevcut private-repository planı branch protection'ı etkinleştirmediği için merge enforcement yoktur. Bu policy sınırı workflow PASS iddiasına dahil değildir.

Protected-catalog projesi solution build'iyle compile edilir; buna karşın `Invoke-WindowsProtectedCatalogSpike.ps1` bu hosted workflow tarafından çağrılmaz ve candidate evidence upload edilmez. Dolayısıyla run number `#24` ve `#25` dahil normal hosted yeşil sonuçlar aday Smoke/`Decision` execution kanıtı sayılmaz.

Workflow'da bilerek üst seviye `paths`/`paths-ignore` filtresi yoktur: required check filtre nedeniyle hiç başlamazsa GitHub check'i `Pending` bırakıp merge'i engelleyebilir. Bu nedenle Windows gate her PR'da sonuç raporlar; ileride maliyet optimizasyonu gerekiyorsa workflow'u skip etmek yerine her zaman sonuç üreten job tasarımı gerekir.

Gerçekten yeşil bitmiş hosted package job'u, yalnız o runner/image ve o commit için signed framework-dependent x64 MSIX üretildiğini; targeted test payload/canary kontrolünü; install, `EnableLUA=1` önkoşulu altında AUMID'in resmi `LOCAL_SERVER` activation API'sinden dönen exact process ID ile görünür launch'ı; basic catalog UIA name/role ve source→category→search Tab focus sırasını; normal close ve exact cleanup zincirini kanıtlar. `ProtectedStoreDirectoryInitialized=true` yalnız exact package-family `LocalCache` path'inin launch sırasında oluştuğunu ve o anda reparse point olmadığını kanıtlar; secret create/read, update/reset/uninstall retention veya deletion/reconciliation kanıtı değildir. Gözlemlenebilen erken process exit'i yalnız exit-code sınıfıyla raporlanır; activation ile process gözlemi arasındaki çok kısa çıkış ayrı bir sabit hata sınıfıdır. Activation tanısı uygulama path'ini, pencere başlığını veya event-log içeriğini sorgulamaz. Data-bearing feature UI davranışı, realized-container/frame, Narrator/genel accessibility, update/migration, WACK veya Store kabulü, non-admin/clean-machine matrisi, gerçek provider/internet, player/codec/HW decode, ARM64 ya da bütün OS/GPU kombinasyonlarını kanıtlamaz. Run `31327398270` için indirilen iki sanitized artifact'ın commit bağı, schema, hash, capability, signature, leak gate, normal-close ve cleanup alanları doğrulanmıştır; bu tarihsel artifact yeni protected-store alanını içermez. Ayrıntılar [M2 completion evidence](../../docs/quality/M2_COMPLETION_EVIDENCE.md) belgesindedir.

## Disposable development identity

Manifestteki `IptvSuite.LocalDev.6f0d9a64` / `CN=IptvSuite Local Development` yalnız yerel M1 test lineage'ıdır. Partner Center rezervasyonu, Store association veya marka hakkı iddiası değildir. Gelecekteki Store identity farklı PFN/AUMID ve app-data alanı oluşturabilir; development paketinden in-place upgrade veya veri migration garantisi yoktur.

Private key, PFX, parola, certificate thumbprint, `Package.StoreAssociation.xml` veya Store identity dosyası commit edilmez. Script wildcard package/certificate silmez.

# M10 native Tier A hosted startup attribution

**Durum:** `SUCCESSOR GRAPH HARD-MAXIMUM OUTLIER ATTRIBUTED; INDEPENDENT-SEGMENT FIXTURE CHECKPOINT PENDING, 2026-08-22`

Bu checkpoint bir acceptance sonucu veya threshold değişikliği değildir. Amacı, hosted Windows Server koşusunda tek startup örneğinin `≤5000 ms` hard maximum sınırını neden az farkla aştığını fixture ya da retry tahminiyle örtmeden ölçmektir.

## Tetikleyen kanıt

`VERIFIED`: Commit `16e22625d4330826ac100648ef21b3038beb8606` için [GitHub Actions run 32540817711](https://github.com/serkankaracan/iptv-suite/actions/runs/32540817711), quality, packaged lifecycle ve DPAPI işlerini geçti. Native probe 100 switch, source-detachment ve surface invariant'larını tamamladı; aggregate startup p95 `2162,9392 ms` idi. Hard maximum `5102,2433 ms` ile `5000 ms` sınırını `102,2433 ms` aştı; HLS p95 `2165,6151 ms`, direct p95 `2162,2629 ms` ölçüldü. Controller success evidence hazırlama ve cleanup publication zincirine ulaşmadığından bu koşu schema-9 hosted PASS değildir.

Önceki bounded rerun `5056,3179 ms` maximum ve `2157,0052 ms` p95 üretmişti. `MediaPlayer.RealTimePlayback=true` sonrasında ilk HLS hard timeout'unun tek koşuda görülmemesi nedensel düzeltme kanıtı değildir; aggregate değerler anlamlı bir iyileşme de göstermez.

`VERIFIED`: Commit `12fac0861f859f379747873c160e054bb95795b8` için [run 32542190663](https://github.com/serkankaracan/iptv-suite/actions/runs/32542190663), quality, package lifecycle ve DPAPI işlerini geçti; native iş ilk switch'te `MediaOpenTimeout` verdi. Runner `Windows Server 2025 Datacenter` idi. HLS manifesti ile dört segment için `5/5` response ve `940229` byte tamamlandı; transport failure ve injected interruption sıfırdı. Probe envelope v2 yalnız tamamlanmış örneğin maximum'unu güncellediğinden ordinal/fixture/attempt/pre-wait/open-wait alanlarının tamamı `0/None` kaldı. Dolayısıyla bu run ağ teslimi eksikliği göstermedi, fakat timeout'un `MediaSource.CreateFromUri`, `Source`, `Play` veya `MediaOpened` wait parçalarından hangisinde zaman harcadığını da ölçemedi.

`VERIFIED`: Commit `c299410d302d6ae2bcf58fe234386afcfdfd8d3a` için [run 32543488245](https://github.com/serkankaracan/iptv-suite/actions/runs/32543488245), locked quality, package lifecycle ve DPAPI işlerini geçti. Native probe 100 switch'i tamamladı; p95 `2150,7287 ms` idi ve retry yapılmadı. İlk HLS/ilk attempt örneği `5049,0157 ms` maximum ile hard tavanı `49,0157 ms` aştı: source-create/assign/`Play` dahil pre-wait `165,1331 ms`, `MediaOpened` wait `4883,8826 ms`, direct maximum `2302,9441 ms` idi. Böylece dominant sürenin `MediaOpenWait` stage'inde olduğu; injected interruption, transport failure, retry ve surface transition gözlenmediği doğrulandı. Bu stage normal loopback teslimi ile parse/demux/decoder/compositor zincirini kendi içinde ayırmadığından network-versus-media-pipeline kök nedeni henüz belirlenmiş değildir. Bu koşu bütçe ihlali nedeniyle schema-9 hosted PASS veya acceptance kanıtı değildir.

`VERIFIED`: Commit `1411472fd4e6be69c21c8b969ef992612fba6f4c` için [GitHub Actions run #148 (`32544835406`)](https://github.com/serkankaracan/iptv-suite/actions/runs/32544835406), locked quality, packaged install/lifecycle ve DPAPI real-user boundary işlerini geçti. Windows Server native işi ilk HLS/attempt-1 örneğinde `MediaOpenTimeout` ile ve `completedSwitches=0` sonucuyla durdu. Active failure snapshot'ı toplam `5154,3957 ms`; source creation `65,7269 ms`, source assignment `53,2906 ms`, `Play` çağrısı `32,1859 ms` ve aktif `MediaOpenWait` `5003,0138 ms` ölçtü. Loopback sunucu `5` isteği kabul edip `5` yanıtı tamamladı; `5` open-ended range ile `940229` byte teslim edildi, `ioAbort=0` ve `transportFailure=0` kaldı. Dolayısıyla `Loaded` sonrasındaki one-shot global `CompositionTarget.Rendered` sınırı ilk HLS timeout'unu gidermedi; ilk-frame lifecycle contention hipotezi yeterli neden olarak reddedilir. Bu hosted native PASS veya acceptance kanıtı değildir.

`VERIFIED (kullanıcının sağladığı manuel transcript; commit/evidence dosyası bağı bağımsız incelenmedi)`: interactive Windows Client üzerinde `SwitchCount=100`, `SoakMinutes=0`, `NetworkInterruptionCount=1` komutu 100 alternating switch ile geçti. Bu kısa koşu controlled interruption/recovery yolunun çalışabildiğini doğrular; hosted Server sonucu, schema-9 hosted success, iki saatlik developer soak veya sekiz saatlik acceptance soak yerine geçmez.

`VERIFIED (ön attribution; successor doğrulaması gerekli)`: Commit `c00a17e642173ee21a0edcb819997bed280730c4` için [GitHub Actions run #149 (`32546119117`)](https://github.com/serkankaracan/iptv-suite/actions/runs/32546119117), locked quality, packaged install/lifecycle ve DPAPI real-user boundary işlerini geçti. Native iş ilk HLS/attempt-1 örneğinde `MediaOpenTimeout` ile ve `completedSwitches=0` sonucuyla durdu. `MediaSource.OpenOperationCompleted`, hata taşımadan `2681,0850 ms` içinde gözlendi; failure snapshot toplam `5079,3715 ms`, source-open sonrası geçen süre `2398,2865 ms` ve aktif `MediaOpenWait` `2396,9873 ms` idi. Loopback sunucu `5` isteği kabul edip `5` yanıtı tamamladı; `940229` byte teslim edildi, `ioAbort=0` ve `transportFailure=0` kaldı. Bu örnek source-open'ın deadline içinde hatasız tamamlandığını, timeout'un sonraki `MediaOpened` bekleyişinde oluştuğunu gösterir. Bağımsız review, ölçüm handler'ının detach öncesinde ayrılması, deadline sonrası event'in reddedilmesi, retry başında snapshot'ın sıfırlanması ve source-open sonrası aktif stage başlangıcının event timestamp'ine bağlanması gereken yarışları saptadığı için bu run yalnız ön attribution kanıtıdır; hosted PASS veya acceptance kanıtı değildir.

`VERIFIED`: Attempt/deadline/cleanup bağını kapatan commit `b4839bfe5c89fed3fb851c68b911181be48c9f6d` için [GitHub Actions run #150 (`32546738817`, attempt 1)](https://github.com/serkankaracan/iptv-suite/actions/runs/32546738817) bütün zorunlu işleri geçti: locked quality, packaged install/lifecycle, DPAPI real-user boundary, native playback ve Required Windows gate yeşildir. Commit-bound schema-9 evidence `100` switch, startup p95 `2178,003 ms`, maximum `4977,728 ms`, HLS/direct p95 `2180,145/2178,003 ms`, `1/1` sentetik interruption/recovery, `0` retry, `100/100` exact source detachment ve altı surface transition kaydetti. Maximum ilk HLS/attempt-1 örneğiydi: pre-wait `93,0410 ms`, `MediaOpened` wait `4884,6865 ms`; attempt-owned source-open event'i hata taşımadan `2467,5278 ms` içinde tamamlandı ve sonraki `MediaOpened` için `2510,1997 ms` geçti. Normal close, process/TLS/package/app-data/certificate/output/environment cleanup, shared Windows App Runtime baseline ve repository-clean kontrollerinin tamamı geçti. Sanitize JSON SHA-256 `77591ca4a2da36d4c5ee431ae703512c1bab578d14493f71447f3a33af590c3e`; GitHub artifact ZIP digest'i `d47e07732b62d9793be08de3252094acae00c4595c037fafcf7ffc5598322703` ve artifact ID `9468881900`dür. Bu, schema-10 cancellation/recovery dilimine geçiş önkoşulunu kapatır; sekiz saatlik soak veya M10 completion kanıtı değildir.

## Ölçüm sınırı

`VERIFIED` statik kod sınırı: `startupStarted`, `MediaSource.CreateFromUri`, `MediaPlayer.Source` ataması ve `Play()` çağrısından önce alınır. Beş saniyelik `WaitAsync` saati ise bu işlemlerden sonra başlar. Run `32543488245`, başarılı bir `MediaOpened` wait'inin kendi beş saniyelik sınırında tamamlanırken toplam source-to-open ölçümünün pre-wait ile birlikte `5000 ms` üzerinde olabildiğini doğruladı. Bu ayrım ölçüm başlangıcını ileri taşımak veya hard maximum'u gevşetmek için kullanılmaz.

Packaged probe envelope schema `3`, tamamlanmış en yavaş örnek için schema 2'deki secretsiz ve bounded alanları korur:

- 1-based switch ordinal'i ve HLS-first ile uyumlu fixture enum'u;
- attempt sayısı ve o switch'teki surface transition sayısı;
- toplamı maximum startup'a eşit pre-wait ile `MediaOpened` wait süreleri;
- HLS ve direct fixture maximum süreleri.

Schema 3 ayrıca tamamlanmamış failure örneğini maximum semantiğine karıştırmadan şu aktif startup snapshot'ını taşır:

- active stage, 1-based switch ordinal'i, fixture, attempt ve switch-local surface transition sayısı;
- toplam aktif startup süresi;
- tamamlanmış source creation, source assignment ve `Play()` çağrı süreleri;
- timeout gözlenirken aktif olan fazın süresi.

Snapshot, inner timeout catch'inde reset/source disposal başlamadan alınır. `MediaOpenTimeout` yalnız source creation/assignment, `Play` invocation, `MediaSourceOpenWait` veya `MediaOpenWait`; `PlaybackAdvanceTimeout` yalnız `PlaybackAdvanceWait`; surface readiness timeout'u yalnız `SurfaceReadiness` stage'i ile eşleşebilir. `MediaSourceOpenWait`, source open-completion event'i henüz gözlenmemişken; `MediaOpenWait`, bu event gözlenip `MediaPlayer.MediaOpened` hâlâ beklenirken kullanılır. Controller exact property/type/enum/parity/attempt/transition/component invariant'larını log üretmeden önce fail-closed doğrular.

Kalıcı success evidence schema `9` ve 71 alan olarak kalır; yalnız `ProbeEnvelopeSchemaVersion=4` olur. Envelope v4, maximum ve active failure için nested source-open diagnostic'inde yalnız completion gözlendi mi, native hata var mı, source-open completion süresi ve completion-sonrası elapsed süreyi taşır. Bu son alan maximum örnekte completion→`MediaOpened`, active failure'da completion→failure snapshot sınırıdır; `MediaOpened` gerçekleştiği iddiası değildir. Deadline sonrasında gelen completion snapshot'a alınmaz, retry attempt'i diagnostic'i sıfırlar ve event handler success/retry source detach'ından önce ya da exception yolunda `finally` ile ayrılır. URI, header, mesaj veya extended native error serialize edilmez. `≤3000 ms` p95, `≤5000 ms` maximum, HLS-first sıra, timeout retry yasağı, tek `MediaPlayer`, network fault sayısı ve required workflow davranışı değişmez.

## Schema-10 successor sınırı

`VERIFIED`: Commit `29bb89ce5a2c411f000c5678f4a4eb482e6a9a61` için [run #151 (`32549714175`)](https://github.com/serkankaracan/iptv-suite/actions/runs/32549714175), predecessor schema-9/envelope-v4 attribution sonucunu değiştirmeden kalıcı evidence schema `10` ve probe envelope v5'i hosted packaged koşuda geçti. Startup p95/maximum `2149.623/4974.486 ms`, HLS/direct p95 `2167.69/2140.485 ms` idi. Tek network interruption/recovery'den bağımsız cancellation probe; exact detach/dispose, `1000 ms` no-auto-restart observation, source-specific `OpenOperationCompleted` ve exact current-source advance ile tamamlandı; retry `0` kaldı. [Cancellation/recovery kaydı](M10_NATIVE_TIER_A_CANCELLATION_RECOVERY_EVIDENCE.md) artifact/JSON digest'lerini ve exact metrikleri taşır. Startup threshold'ları ile yalnız-explicit-`MediaFailed` retry kuralı değişmez; bu native API cancellation, OS session quiescence, M13 reconnect ya da M10 completion kanıtı değildir.

`VERIFIED — successor graph reproduction`: Windows App SDK `2.4.0 → WinUI 2.3.6` ve fail-closed signed-package inventory düzeltmesini taşıyan commit `0503ed7cd983ad0b1062dd768eeb69b448d33165` için [run `32555764285`](https://github.com/serkankaracan/iptv-suite/actions/runs/32555764285), locked quality, packaged install/lifecycle ve DPAPI real-user boundary işlerini geçti; gerçek imzalı MSIX inventory adımı da iki native attempt'te geçti. Native attempt 1 ilk HLS/attempt-1'de toplam `5103,4564 ms`, hatasız source-open `2454,8078 ms` ve source-open sonrası `2648,6486 ms` ile `MediaOpenTimeout` verdi. Aynı committeki tek bounded failed-job rerun'u toplam `5139,9608 ms`, hatasız source-open `2567,7833 ms` ve sonraki `2572,1775 ms` ile aynı failure domain'ini yeniden üretti. Her iki attempt de `5/5` loopback response, `940229` byte, `ioAbort=0` ve `transportFailure=0` kaydetti. Bu nedenle sonuç inventory, eksik transfer veya source-open hatası değildir; `MediaOpened` completion/deadline sınırında tekrarlanabilir hosted Windows Server ihlalidir. Required gate yalnız native iş nedeniyle kırmızıdır ve bu kayıt PASS/acceptance değildir.

`VERIFIED — envelope v6 result`: Commit `78ff98e4972bb075977af04597e8717c692b0f21` için [run #155 (`32557484626`)](https://github.com/serkankaracan/iptv-suite/actions/runs/32557484626), locked quality, packaged install/lifecycle, DPAPI real-user boundary ve gerçek signed-MSIX inventory adımlarını geçti. Native probe 100 switch'i tamamladı ve timeout yoluna girmedi; p95 `2160,3704 ms`, ilk HLS/attempt-1 maximum `5024,5709 ms`, pre-wait `86,1858 ms` ve `MediaOpened` wait `4938,3851 ms` oldu. Source-open hata olmadan `2454,8193 ms` içinde tamamlandı; source-open sonrasından `MediaOpened` event'ine `2569,7516 ms` geçti. HLS/direct maximum değerleri `5024,5709/2286,6558 ms`, retry `0` idi. V6 timeout-only alanları bu success-path budget failure'ında tasarlandığı gibi neutral kaldı. Bu koşu kayıp event/deadline yarışı değildir: `MediaOpened` mevcut wait içinde geldi, fakat source creation öncesinde başlayan toplam kronometre hard `5000 ms` sınırını `24,5709 ms` aştı. Required gate yalnız native budget sonucu nedeniyle kırmızıdır; koşu PASS veya acceptance değildir.

`VERIFIED — hosted negative A/B`: Independent-segments sözleşmesini taşıyan commit `e517fa8c86b6557422658e7457363806c5a9f84e` için [run #156 (`32559852397`)](https://github.com/serkankaracan/iptv-suite/actions/runs/32559852397), locked quality, packaged install/lifecycle, DPAPI real-user boundary ve signed inventory adımlarını geçti; native probe `100` switch'i tamamladı. P95 `2156,3241 ms` geçti, ancak ilk HLS/attempt-1 maximum `5105,5754 ms` ile hard toplam tavanı `105,5754 ms` aştı. Pre-wait `122,5842 ms`, `MediaOpened` wait `4982,9912 ms`, hatasız source-open `2518,1525 ms`, source-open sonrası `MediaOpened` `2587,4229 ms`, direct maximum `2295,4649 ms` ve retry `0` idi. Event kendi wait'i içinde geldiği için sonuç timeout/lost-event yarışı değil, total startup-budget ihlalidir. Independent-segments metadata'sı geçerli fixture hardening'i olarak kalır fakat tek başına yeterli startup düzeltmesi değildir. Run #155 ile tek maximum örnekleri arasındaki fark istatistiksel regresyon kanıtı sayılmaz; native ve Required gate kırmızı olduğundan success artifact'ı yayımlanmadı.

`IMPLEMENTED LOCALLY; HOSTED A/B PENDING — zero-timeline remux`: Direct TS üretimi zaten `-muxdelay 0 -muxpreload 0` kullanırken HLS remux bu output seçeneklerini taşımıyordu. Pinned `ffprobe`, önceki HLS ilk video PTS/DTS dizisini `1,480/1,400`, `3,480/3,400`, `5,480/5,400`, `7,480/7,400` saniye; direct başlangıcını `0,080/0,000` saniye ölçtü. Aynı seçenekler HLS remux'a eklendiğinde video başlangıçları direct ile `0,080/0,000` tabanına ve iki saniyelik cadence'e, ilk HLS audio ise direct `0,058667/0,058667` tabanına hizalandı [S129]. Generator sonlu first-packet değerlerine ek olarak iki stream'in complete `time_base` + integer PTS/DTS/duration/flags dizisini ve çıkarılmış elementary-stream eşliğini direct/HLS arasında fail-closed doğrular. H.264 (`793276` byte, SHA-256 `151ed70976e6c512e83fd18c36b92acf2f8fc76aff0f3b2c545385e1fef8f8c9`) ve AAC (`99019` byte, SHA-256 `ad48b7256107b94184f908b9cd6a16948db9516511b62624dc51e54ed1c87b40`) direct/HLS arasında byte-identical kaldı; dört TS toplamı küçülmek yerine `940000 → 978164` byte oldu. Bu, aynı sentetik sunumun container-timeline parity düzeltmesi ve bounded latency hipotezidir; hosted etki henüz `UNVERIFIED`dır. Arbitrary positive-base HLS desteğini, genel HLS uyumluluğunu veya M10 kabulünü kanıtlamaz.

`VERIFIED — local pre-commit`: Exact SDK `10.0.302` ile Windows PowerShell 5.1 generator iki ardışık koşuda yedi fixture dosyasını byte-identical üretti; LF manifest SHA-256 `daee6e27dc686d365e49ae750eaa7447882ec48df0e87286d5548578b682f778` oldu. Debug/Release x64 build `0` warning/error; architecture `29`, unit `171`, integration `106` olmak üzere `306/306 × 2` identical test seti, fixture determinism, sentinel fail/recovery ve canary kontrolleri geçti. Dirty-worktree summary `commitSha=null`, `29206` byte ve SHA-256 `278604f33800075cf019a22fd01119599386932d98e14c90f9184c6230602491` taşır; bu sonuç commit-bound hosted veya packaged playback PASS değildir.

## Karar sınırı

Bu instrumentation performans bütçesini yükseltmeyi, ölçüm başlangıcını ileri taşımayı, warm-up ile ilk örneği dışlamayı veya timeout retry eklemeyi yasaklayan mevcut kabul sınırını korur.

Run #148, `CompositionTarget.Rendered` davranışının bu dar startup hipotezi için çözüm olmadığını doğruladığından bu davranış takip checkpoint'inden çıkarıldı. Run #149'daki salt diagnostic, `MediaSource.OpenOperationCompleted` sonucunu ve zamanını active failure snapshot'ına bağlayarak source-open tamamlanması ile sonraki `MediaOpened` bekleyişini ayırdı [S120]. Handler attempt'e aittir, Source atamasından önce bağlanır, deadline ile sınırlandırılır ve success/retry detach'ından önce; exception yolunda ise reset/source disposal öncesinde idempotent `finally` korumasıyla ayrılır. Bu event tek başına playback readiness veya başarı garantisi değildir; yeni retry, warm-up/sample exclusion ya da alternatif güvenlik yolu açmaz.

`≤3000 ms` p95, `≤5000 ms` maximum, source creation öncesindeki ölçüm başlangıcı, HLS-first sıra, tek player, yalnız explicit ilk `MediaFailed` retry kuralı, TLS/loopback sınırı ve fail-closed evidence/cleanup sözleşmeleri aynen korunur. Run #150 attempt/deadline/cleanup bağını ve schema-9 hosted success yolunu doğruladı; source-open sonrası media pipeline iç fazı hâlâ daha dar sınıflandırılmadığından bu tek başarı kök nedeni kapatmaz. Schema-10 cancellation/recovery başlayabilir. Sekiz saatlik acceptance soak kullanıcı tarafından açıkça başlatılmadı ve bu checkpoint kapsamında başlatılmaz.
